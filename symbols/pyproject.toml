[project]
name = "symbols"
version = "0.1.0"
description = "Extract Ethiopian party ballot symbols from the NEBE PDF"
requires-python = ">=3.14"
dependencies = [
    "pillow>=12.2.0",
    "pymupdf>=1.27.2.3",
]

[dependency-groups]
dev = [
    "ruff>=0.15.13",
    "ty>=0.0.38",
]
